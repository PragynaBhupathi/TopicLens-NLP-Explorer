version: "3.9"

services:
  topic-modeling:
    build: .
    container_name: topic-modeling-nlp
    ports:
      - "5000:5000"
    volumes:
      - ./outputs:/app/outputs        # persist trained models & visuals
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
